import '../globals.css'

export const metadata = {
  title: 'AI 놀자 - 에디터',
  description: '페이지 빌더 에디터',
}

export default function EditorLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
